# Example environment configuration
# Copy this file to .env and customize as needed

# Agent weights (higher = more influence on final decision)
MOMENTUM_AGENT_WEIGHT=1.0
VALUE_AGENT_WEIGHT=1.0
RISK_AGENT_WEIGHT=1.2

# Data fetching parameters
DEFAULT_PERIOD=1y
DEFAULT_INTERVAL=1d

# Risk management parameters
MAX_VOLATILITY=0.30

# Momentum agent parameters
MOMENTUM_SHORT_WINDOW=20
MOMENTUM_LONG_WINDOW=50

# Value agent parameters
VALUE_LOOKBACK_PERIOD=100
